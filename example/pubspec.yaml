name: fan_deck_otp_example
description: Demonstrates how to use the fan_deck_otp package with animations, themes, and controller interactions.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.11.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  fan_deck_otp:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
